interface:
  display_name: "Cross-agent SDD"
  short_description: "Bootstrap shared SDD governance across coding agents"
  default_prompt: "Use $cross-agent-sdd-skill to audit this repository and propose a safe cross-agent SDD bootstrap."

policy:
  allow_implicit_invocation: true
